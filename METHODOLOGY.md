# Methodology

This document is a placeholder. The full methodology is written in commit 5 of the scaffolding sequence. The same prose is mirrored at `/methodology` on the live site so readers can find it without leaving the page.

Until then, see `BRIEF.md` Sections 5.2 and 5.3 for the status taxonomy and source hierarchy that the editors apply.
